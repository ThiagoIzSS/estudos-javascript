let forma = 'retângul';
let altura = 10;
let base = 2;
let area = 0;

if (forma === 'retângulo') {
    area = base * altura
    console.log(`A área do retângulo é ${area}`)
} else {
    area = base * altura/2
    console.log(`A área do triângulo é ${area}`)
}
